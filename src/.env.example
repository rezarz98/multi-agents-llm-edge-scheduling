
# Template — copy to the PROJECT ROOT as `.env` and fill in your key.
#   cp src/.env.example ../.env    (from the src/ directory)
# The real .env belongs at the project root (outside this git repo) and is
# git-ignored. Never commit a file containing a real key.
ANTHROPIC_API_KEY=
